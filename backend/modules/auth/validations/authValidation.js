// Validation rules for auth endpoints
// For now, we'll use basic validation in controller
// Can be expanded later with proper validation library

module.exports = {
  login: [],
  register: [],
};
